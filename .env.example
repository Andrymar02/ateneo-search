# Copia questo file in .env (ignorato da git) e personalizza se serve.
# Percorso dell'indice SQLite+sqlite-vec da usare per le ricerche.
INDICE_DB=data/index/idx_bge-m3_cs300_ov50.db
# Origine da cui accettare richieste (il frontend Vite in sviluppo).
FRONTEND_ORIGIN=http://localhost:5173
